import { VariableEditorDirective } from './variable-editor.directive';

describe('VariableEditorDirective', () => {
  it('should create an instance', () => {
    const directive = new VariableEditorDirective();
    expect(directive).toBeTruthy();
  });
});
